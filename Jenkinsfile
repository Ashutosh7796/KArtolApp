pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building application'
                // sh 'mvn clean package'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying application on local machine'
            }
        }
    }
}
