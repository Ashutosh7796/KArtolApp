server.port= 8080

#datasource
spring.datasource.url=jdbc:mysql://localhost:3306/PdfGenerator?createDatabaseIfNotExist=true&useSSL=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.generate-ddl=true
spring.jpa.show-sql=true

spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.MySQL8Dialect

spring.jpa.hibernate.ddl-auto=update

spring.main.allow-circular-references=true

spring.mail.username=ashutoshshedgeas87@gmail.com
spring.mail.password=kpwb azsr geda erhx

spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.auth=true

# Swagger properties
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs
app.server.url=http://localhost:8080

# Rate limiting settings
app.rate-limiting.enabled=true
app.rate-limiting.limit-for-period=20
app.rate-limiting.refresh-period=60
app.rate-limiting.timeout-duration=300

spring.task.scheduling.time-zone=Asia/Kolkata

# Password reset URL
app.url.password-reset=http://localhost:3000/reset-password

# Jasypt Encryption Configuration
jasypt.encryptor.password=secure-encryption-key-123
jasypt.field-encryptor.password=secure-field-encryption-key-456

# Enable encryption migration on startup (set to true to migrate data)
encryption.migration.enabled=true

# Enable encryption test utility (set to true to run tests on startup)
encryption.test.enabled=true

# Set logging level for encryption classes
logging.level.com.spring.jwt.utils.encrypt=DEBUG

